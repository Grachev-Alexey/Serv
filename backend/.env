# СЕКРЕТЫ — НЕ коммитить (файл в .gitignore). Для прода задать свои значения.

# PostgreSQL (сервер). Пароль сменить после того, как он засветился в переписке.
DATABASE_URL=postgresql+psycopg2://postgres:cd5d56a8@213.226.124.2:5432/camera

# Токен, которым камеры авторизуют загрузку (POST /api/upload)
UPLOAD_TOKEN=test_token_123

# Секрет для подписи JWT-сессий панели (в проде — длинная случайная строка)
JWT_SECRET=dev-only-change-me-in-production

# Учётка администратора, создаётся при первом старте
ADMIN_USER=vivi-admin
ADMIN_PASSWORD=vivimonitoring

# Cookie: в проде за HTTPS выставить true
COOKIE_SECURE=false

# Разрешённые источники для CORS (дев-фронт Vite)
ALLOWED_ORIGINS=http://localhost:5173

# Пути к ffmpeg/ffprobe (на Ubuntu достаточно ffmpeg/ffprobe в PATH)
FFMPEG_BIN=c:\Users\grach\OneDrive\Рабочий стол\server\tools\ffmpeg-8.1.2-essentials_build\bin\ffmpeg.exe
FFPROBE_BIN=c:\Users\grach\OneDrive\Рабочий стол\server\tools\ffmpeg-8.1.2-essentials_build\bin\ffprobe.exe


# ── ИИ-модели и режим анализа (правка 2026-09-08) ───────────────────────
# был mistral-large-latest → 403 (нет в тарифе)
MISTRAL_MODEL=ministral-14b-2512
# был pixtral-large-latest → 400 (модели не существует)
VISION_MODEL=ministral-14b-2512
# работает, лимит 60 req/min
MISTRAL_EMBED_MODEL=mistral-embed
# было 12 → пачки по 3 строки; теперь ~20 вызовов/сутки вместо 350
COARSE_GAP_MINUTES=90
# было 5
CONV_SETTLE_MINUTES=20
# было 6 — это 3/4 всего трафика
VISION_SAMPLES=2
# было 3
VISION_SCAN_SAMPLES=2
